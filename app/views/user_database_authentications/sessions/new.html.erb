<div class="min-h-screen flex items-center justify-center bg-stone-50 px-4 py-8">
  <div class="w-full max-w-md">
    <!-- ロゴ・ヘッダー部分 -->
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-stone-900 leading-tight mb-2">
        Family App
      </h1>
      <p class="text-base text-stone-600">
        ログインして家族の空間にアクセス
      </p>
    </div>

    <!-- ログインカード -->
    <div class="bg-white rounded-xl shadow-sm border border-stone-100 p-6 md:p-8">
      <h2 class="text-2xl font-semibold text-stone-800 mb-6">
        ログイン
      </h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-4" }) do |f| %>
        <!-- メールアドレス -->
        <div class="space-y-2">
          <%= f.label :email, "メールアドレス", class: "block text-sm font-semibold text-stone-700" %>
          <%= f.email_field :email,
            autofocus: true,
            autocomplete: "email",
            placeholder: "your@email.com",
            class: "w-full px-4 py-3 text-base text-stone-800 placeholder-stone-400 bg-white border-2 border-stone-200 rounded-lg transition-all duration-200 focus:outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100 min-h-[44px]" %>
        </div>

        <!-- パスワード -->
        <div class="space-y-2">
          <%= f.label :password, "パスワード", class: "block text-sm font-semibold text-stone-700" %>
          <%= f.password_field :password,
            autocomplete: "current-password",
            placeholder: "••••••••",
            class: "w-full px-4 py-3 text-base text-stone-800 placeholder-stone-400 bg-white border-2 border-stone-200 rounded-lg transition-all duration-200 focus:outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100 min-h-[44px]" %>
        </div>

        <!-- ログイン状態を保持 -->
        <% if devise_mapping.rememberable? %>
          <div class="flex items-center gap-3 py-2">
            <%= f.check_box :remember_me,
              class: "w-6 h-6 text-teal-500 bg-white border-2 border-stone-300 rounded transition-all duration-200 focus:ring-4 focus:ring-teal-100 cursor-pointer" %>
            <%= f.label :remember_me, "ログイン状態を保持する", class: "text-base text-stone-800 select-none cursor-pointer" %>
          </div>
        <% end %>

        <!-- ログインボタン -->
        <div class="pt-2">
          <%= f.submit "ログイン",
            class: "w-full bg-orange-400 hover:bg-orange-500 active:bg-orange-600 text-white font-semibold px-6 py-3 rounded-lg shadow-sm hover:shadow-md transition-all duration-200 ease-out active:scale-95 focus:outline-none focus:ring-4 focus:ring-orange-200 disabled:opacity-50 disabled:cursor-not-allowed min-h-[44px]" %>
        </div>
      <% end %>

      <!-- リンク -->
      <div class="mt-6 pt-6 border-t border-stone-200">
        <p class="text-sm text-stone-600 text-center">
          アカウントをお持ちでないですか？
          <%= link_to "招待リンクから登録", new_user_database_authentication_registration_path,
            class: "text-orange-500 hover:text-orange-600 font-semibold transition-colors duration-200" %>
        </p>
      </div>
    </div>

    <!-- フッター -->
    <div class="text-center mt-6">
      <p class="text-xs text-stone-500">
        © 2025 Family App. All rights reserved.
      </p>
    </div>
  </div>
</div>
